\chapter{Implementación}\label{cap:implementacion}

\section{Decisiones de Diseño}
Al tratarse de un proyecto centrado en la investigación y la inteligencia artificial y no en el desarrollo de una aplicación clásica, el diseño se enfoca en la robustez del tratamiento de los datos, la causalidad física de los modelos y la estructuración de un \textit{pipeline} analítico reproducible.

\subsection{Arquitectura}
A diferencia de las arquitecturas de \textit{software} tradicionales, los sistemas de \textit{Machine Learning} modernos orientados a la investigación se diseñan habitualmente como \textbf{pipelines de datos estructurados}. Para este proyecto, la arquitectura se ha materializado en un ecosistema de \textbf{cuadernos de experimentación interactivos (Jupyter Notebooks)} apoyados por scripts modulares de utilidades.

Esta decisión arquitectónica de tratar cada experimento como un ``módulo'' independiente dentro del sistema se justifica por las siguientes ventajas técnicas:
\begin{itemize}
	\item \textbf{Aislamiento de entornos de ejecución:} El entrenamiento de modelos como XGBoost o arquitecturas profundas en TensorFlow requiere un manejo intensivo de la memoria RAM. Al separar cada enfoque predictivo en un cuaderno distinto, se aísla el estado de las variables y se previenen desbordamientos de memoria o conflictos entre librerías.
	\item \textbf{Interactividad y trazabilidad computacional:} La naturaleza de celda a celda de los cuadernos permite ejecutar, visualizar y validar el progreso de los experimentos. Esto resulta vital para la ingeniería de datos y la detección visual de anomalías antes de someter los datos a entrenamientos computacionalmente costosos.
	\item \textbf{Documentación integrada:} Los cuadernos permiten entrelazar la lógica de programación en Python con bloques explicativos en \textit{Markdown} y salidas gráficas. Cada cuaderno actúa, por tanto, como un módulo de ejecución y como el propio reporte de resultados.
\end{itemize}

\subsection{Tecnologías y Herramientas}
Para la implementación de los requisitos recogidos y la construcción de los cuadernos, se ha seleccionado un conjunto de herramientas pertenecientes al ecosistema estándar de la ciencia de datos en Python:
\begin{itemize}
	\item \textbf{Lenguaje de programación:} Seleccionado \textbf{Python 3} \cite{descarga-python} como lenguaje principal por su hegemonía en el desarrollo de algoritmos de inteligencia artificial y su extensa comunidad de soporte.
	\item \textbf{Entorno de desarrollo (IDE):}
		\subitem \textbf{Jupyter Notebooks:} \cite{descarga-jupyter} Plataforma de computación interactiva que permite la creación de los módulos de experimentación, combinando código, texto y visualizaciones.
		\subitem \textbf{PyCharm:} \cite{descarga-pycharm} Es un entorno de desarrollo integrado (IDE) diseñado por la compañía JetBrains 
		específicamente para el lenguaje de programación Python. Posee herramientas de análisis y depuración de código, además de integración con sistemas de control de versiones.
	\item \textbf{Librerías de datos y álgebra:}
		\subitem \textbf{Numpy:} Numpy \cite{documentacion-numpy} es una librería de Python que le agrega mayor soporte para vectores y matrices, constituyendo una biblioteca de funciones matemáticas de alto nivel.
		\subitem \textbf{Pandas:} \cite{documentacion-pandas} Es una biblioteca de software escrita como extensión de Numpy para manipulación y análisis de datos. Ofrece estructuras de datos y operaciones para manipular tablas numéricas y series temporales.
	\item \textbf{Visualización de datos:}
		\subitem \textbf{Matplotlib:} \cite{documentacion-matplotlib} Librería de graficación bidimensional, capaz de generar representaciones gráficas estáticas, animadas e interactivas.
		\subitem \textbf{Seaborn:} \cite{documentacion-seaborn} Construida sobre Matplotlib, proporciona una interfaz de alto nivel para trazar gráficos estadísticos atractivos e informativos.
	\item \textbf{Librerías de modelos predictivos:}
		\subitem \textbf{Scikit-Learn:} \cite{documentacion-scikit-learn} Es una librería que incluye varios algoritmos de clasificación, regresión y clustering. Además, implementa gran parte de lógica necesaria para la normalización, creación de particiones y cálculo de métricas.
	\item \textbf{Deep Learning:}
		\subitem \textbf{TensorFlow y Keras:} TensorFlow \cite{documentacion-tensor-flow} y Keras \cite{documentacion-keras} son Frameworks desarrollados por Google utilizados para construir, compilar y entrenar Redes Neuronales mediante su API funcional.
	\item \textbf{Optimización y búsqueda de hiperparámetros:}
		\subitem \textbf{Keras Tuner:} Keras Tuner \cite{documentacion-keras-tuner} es una librería específica de optimización para modelos de \textit{Deep Learning}.
		\subitem \textbf{MetaGen:} \cite{github-metagen} Framework de optimización herística y metaheurística.
	\item \textbf{Persistencia de modelos:}
		\subitem \textbf{Joblib:} \cite{documentacion-joblib} Es una biblioteca que proporciona herramientas para facilitar y optimizar la ejecución de código. Además, permite la persistencia de modelos.
\end{itemize}

\section{Desarrollo}

\subsection{Adquisición de Datos}
Como se ha expuesto anteriormente, para resolver el problema propuesto es necesario disponer de un gran volumen de datos. Inicialmente se dispone de datos reales de \textit{P} y \textit{Q} en los nudos de la red \ref{fig:red-tfg} para el periodo de un día y con una frecuencia de cinco minutos. A partir de los datos de P y Q, resolviendo un flujo de cargas, se obtiene el estado de tensiones en los nudos de la red, los flujos de potencia y corrientes. Finalmente con todo esto se deducen las pérdidas del circuito. 

\begin{figure}[htp]
	\centering
	\includegraphics[width=0.9\textwidth]{figures/red_tfg.png}
	\caption{Red objeto de estudio. Fuente: Recurso proporcionado por tutores}
	\label{fig:red-tfg}
\end{figure}

La red de distribución objeto de estudio está compuesta por dos subsistemas y 14 nudos (N1 a N14). Entre sus componentes destacan los generadores distribuidos, identificados mediante iconos representativos de su fuente de energía primaria.

Dado que los datos de un único día son insuficientes para entrenar modelos de inteligencia artificial y obtener resultados satisfactorios, se ha recurrido a  \textit{scripts} de Python heredados que generan datos de consumo y generación para un año completo. El proceso de generación consiste en aplicar a los datos de \textit{P} y \textit{Q} ya disponibles, variaciones aleatorias proporcionales a la potencia máxima de la carga o la generación. 

A continuación, estos datos se procesan mediante la herramienta de modelado y optimización GAMS. Es fundamental destacar que los \textit{scripts} heredados en GAMS no se limitan a resolver un flujo de cargas estándar para obtener el estado de la red. Por el contrario, ejecutan un \textbf{Flujo de Cargas Óptimo (OPF)}. El objetivo de esta optimización es encontrar la configuración óptima que minimice las pérdidas totales del sistema. De este proceso se extraen los atributos de entrada para los modelos (las potencias en los nudos) y las variables objetivo que se pretenden predecir (las tensiones óptimas de los nudos y las pérdidas mínimas resultantes).

Esta distinción es precisamente lo que separa este proyecto del TFM predecesor. Mientras que aquel se centraba en la predicción del estado de la red (resolución directa de flujos), este persigue el control activo de esta para optimizar su eficiencia. 

La estrategia de control se basa en la inyección estratégica de potencia reactiva mediante los inversores de los generadores. Al inyectar potencia reactiva localmente, se elevan las tensiones en el nudo y en sus adyacentes. Si esta inyección se realiza cerca de los puntos con alta demanda reactiva, se evita que dicha potencia deba ser transportada desde el transformador principal de la subestación. Esta compensación local reduce drásticamente la corriente total (\textit{I}), lo que se traduce en una minimización de las pérdidas del sistema, proporcionales a $I^2R$.

En los \textit{scripts} de resolución del OPF en GAMS, por simplicidad y limitación de la herramienta, solo se tiene en cuenta la optimización de inyección de potencia reactiva por el generador de energía eólica (icono en verde). Por tanto, las pérdidas del sistema y tensiones calculadas por este método, se corresponden con los valores óptimos obtenidos al inyectar la potencia reactiva correspondiente en el sistema por ese generador.

De esta forma en la figura \ref{fig:dataset-1}, podemos observar la forma del \textit{dataset} con los atributos de entrada. Se ha recortado el contenido de este para mostrar la información de los primeros 7 nudos. La primera columna llamada ``Column1'' muestra solo la indexación de cada muestra con el formato $dayX\_minY$, siendo \textit{X} el día e \textit{Y} el minuto del instante recogido. Cada columna $PZ$ y $QZ$ corresponden a la potencia activa y potencia reactiva del nudo \textit{Z}.

\begin{figure}[htp]
	\centering
	\includegraphics[width=0.9\textwidth]{figures/dataset_entrada.png}
	\caption{Dataframe del dataset de entrada. Fuente: Elaboración propia}
	\label{fig:dataset-1}
\end{figure}

En la figura \ref{fig:dataset-2} se puede observar la forma del \textit{dataset} con los atributos de salida. Presenta de nuevo la misma indexación. La columna QGEN7 corresponde con la cantidad de potencia reactiva inyectada. Las columnas con el formato $VX$ muestran la tensión del nudo \textit{X} y la columna PERD las pérdidas del sistema, correspondientes tras inyectar dicha potencia reactiva. Además de las columnas que se observan, también se disponen de las intensidades de corrientes en las líneas entre cada nudo. Sin embargo, como se explicará en siguientes secciones, estas columnas no se utilizarán.
\begin{figure}[htp]
	\centering
	\includegraphics[width=0.9\textwidth]{figures/dataset_salida.png}
	\caption{Dataframe del dataset de salida. Fuente: Elaboración propia}
	\label{fig:dataset-2}
\end{figure}

\subsection{Procesamiento de Datos}
Esta sección pretende adentrarse en la ingeniería de datos y abordar el tratamiento de esta información para el entrenamiento de los diferentes modelos. Para ello, primero debemos tener una visión general de los datos que disponemos. En la figura \ref{fig:hist_input} se encuentran los histogramas de todas las columnas de los atributos de entrada y en la figura \ref{fig:hist_output} los histogramas de los atributos de salida.

En la figura \ref{fig:hist_input}, podemos observar como las variables P2 y Q2 tienen un valor constante en todas las muestras. Esto es debido a que el nudo 2 es un nudo de tránsito. Un nudo de tránsito es un punto de la red eléctrica donde se unen dos o más líneas, pero no hay ninguna carga (consumidor) ni ningún generador conectado a él. Dado que estas variables son en realidad constantes, lo mejor es eliminar estas columnas del \textit{dataset} para el entrenamiento. Estos datos no presentan ningún patrón que aprender, por lo que solo ralentizan el proceso de aprendizaje en los modelos, al tener que ajustar igualmente el peso de estos, lo que conlleva un mayor consumo de recursos innecesarios. 

\begin{figure}[htp]
	\centering
	\includegraphics[width=0.9\textwidth]{figures/hist_data_input.png}
	\caption{Histogramas de atributos de entrada. Fuente: Elaboración propia}
	\label{fig:hist_input}
\end{figure}

\clearpage

Analizando el rango de cada una de las variables de entrada, podemos observar como algunas presentan rangos muy amplios, como el nudo 12 (con potencia activa entre 0 y 30), mientras que otros presentan rangos más estrechos (entre 0 y 1). Si estos datos se introducen en crudo, ciertos algoritmos matemáticos basados en el descenso de gradiente o cálculo de distancias (como las Redes Neuronales) tenderán a otorgar un mayor peso a la variables con números mayores, opacando la influencia real en el resto de la red. Para mitigar este problema, es común realizar una \textbf{normalización o estandarización} de las variables. Al escalar todas las entradas a un rango común, se garantiza un marco de evaluación equitativo, asegurando que las métricas de error resultantes reflejen la verdadera capacidad de aprendizaje de cada arquitectura y no su sensibilidad frente a datos sin procesar. Por el contrario, los modelos de ensamblaje basados en árboles de decisión son matemáticamente invariantes a la escala monótona, por lo que son capaces de procesar las magnitudes físicas en bruto sin sufrir este sesgo. Siguiendo este criterio, finalmente se decidió normalizar las entradas solo con los modelos de Redes Neuronales y dejar los datos en bruto para el resto de algoritmos.

En la figura \ref{fig:hist_output}, podemos observar el mismo comportamiento constante con la variable V1, por lo que esta columna también se descartará. En cuanto a las valores de las variables que representan las intensidades de corriente en cada línea, muchos de estos valores son nulos (I0\_1 son todos valores nulos) y presentan rangos muy estrechos. En este caso estas variables se descartarán y no se aprenderán por las siguientes razones:
\begin{itemize}
	\item La mayoría de estos modelos no compilan trabajando con nulos de por medio. Para tenerlos en cuenta, habría que rellenar estos valores y con variables tan delicadas como las eléctricas, puede ser contraproducente si se insertan valores incorrectos.
	\item Como se explicó con el nudo de tránsito, si se introduce una variable con una baja varianza, se introduce ruido en el proceso de aprendizaje.
	\item Al reducir el número de variables a predecir, se simplifica la superficie de error del modelo.
	\item A partir de las potencias y las tensiones, ya es posible calcular fácilmente las intensidades.
\end{itemize}

\begin{figure}[htp]
	\centering
	\includegraphics[width=0.9\textwidth]{figures/hist_data_output.png}
	\caption{Histogramas de atributos de salida. Fuente: Elaboración propia}
	\label{fig:hist_output}
\end{figure}

\clearpage

Analizando la variable de pérdidas de la red, observamos que presenta valores negativos. Esto es un error debido a la no convergencia del problema de optimización en GAMS, por lo que se deben filtrar las muestras que no tengan estas pérdidas negativas como variable objetivo.

Por otro lado, la variable correspondiente a la inyección de potencia reactiva revela un comportamiento marcadamente discreto. Debido a la resolución numérica del problema de flujo de cargas óptimo ejecutada por el software GAMS, los valores de esta variable varían mediante saltos escalonados en intervalos de 0,01. A pesar de este comportamiento discreto, esta variable no debe de abordarse como un problema de clasificación. En primer lugar, la potencia reactiva es una magnitud físicamente continua. En segundo lugar, los modelos de clasificación estándar no evalúan la magnitud del error entre clases, penalizando por igual una desviación mínima (ej. predecir 0,06 en lugar de 0,05) que un error máximo. Por tanto, el enfoque de regresión es el idóneo matemáticamente, ya que minimiza la distancia real del error, permitiendo aplicar un redondeo en el postprocesamiento si el sistema requiere verdaderamente de un comportamiento discreto.

La normalización de las variables de entrada es un proceso casi obligatorio en el \textit{Machine Learning}, sin embargo, este proceso no tiene por qué aplicarse a las variables objetivo. Si bien hay ciertos modelos que requieren también de esta doble normalización (Redes Neuronales), a menos que se intenten predecir diferentes variables con rangos muy distintos, este proceso no es aconsejable. Las métricas de evaluación de estos modelos, de las que se indagará en futuras secciones, tratan de medir la diferencia entre el valor esperado y el predicho. Si se normalizan las variables estas diferencias pierden significado en el problema, mientras que si se mantienen, es posible hablar en unidades físicas reales. En este caso las tensiones tienen un rango aproximado entre 0 a 1 y las pérdidas positivas un rango entre 0 y 20, por lo que se tratará de preservar la distribución de estas variables.

En las figuras \ref{fig:hist_clean_input} y \ref{fig:hist_clean_output}, se pueden observar los histogramas de las variables del \textit{dataset} una vez tratadas. Estos valores son los que se usarán en el entrenamiento de los diferentes modelos. En total, después de este tratamiento, el conjunto de entrenamiento se compone de 104.936 muestras. En el extracto de código \ref{cod:clean_dataset}, se expone el tratamiento que se ha realizado para la limpieza del conjunto de datos. 

\begin{figure}[htp]
	\centering
	\includegraphics[width=0.9\textwidth]{figures/hist_clean_data_input.png}
	\caption{Histogramas de atributos de entrada limpios. Fuente: Elaboración propia}
	\label{fig:hist_clean_input}
\end{figure}
\begin{figure}[htp]
	\centering
	\includegraphics[width=0.9\textwidth]{figures/hist_clean_data_output.png}
	\caption{Histogramas de atributos de salida limpios. Fuente: Elaboración propia}
	\label{fig:hist_clean_output}
\end{figure}
\begin{lstlisting}[language=Python, caption={Código de preprocesamiento del dataset}, label={cod:clean_dataset}, captionpos=b]
	X = pd.read_csv('../dataset/dataset_in.txt', index_col=0)
		.drop(columns=['P2', 'Q2'])
	y_columns = [f'V{i}' for i in range(2,15)] + ['PERD', 'QGEN7']
	Y = pd.read_csv('../dataset/dataset_out.txt', index_col=0)
		.loc[:, y_columns]
	
	mask = Y['PERD'] >= 0
	X = X[mask]
	Y = Y[mask]
\end{lstlisting}

\clearpage

\subsection{Enfoque del Entrenamiento}
Antes de entrar en el entrenamiento de cada modelo, definiremos diferentes decisiones que se han tomado y técnicas utilizadas para cada uno de estos entrenamientos.

En anteriores secciones, se ha comentado la necesidad de realizar una partición del conjunto de datos en dos subconjuntos independientes. Hay una amplia variedad de técnicas que tienen como objetivo esta división, pero principalmente se pueden resumir en dos enfoques: 
\begin{itemize}
	\item \textbf{Validación por retención (\textit{holdout validation}):} Consiste en dividir el conjunto de datos en dos subconjuntos: un subconjunto de entrenamiento a partir del cual se construirá el modelo y un subconjunto de prueba del que se calcula el rendimiento del modelo. La separación de estos conjuntos se realiza generalmente de manera aleatoria, asignando un porcentaje mayoritario al entrenamiento (típicamente entre el 70\% y el 80\%).
		\subitem \textbf{Ventajas:} Es un método computacionalmente muy eficiente y rápido de ejecutar, ya que el modelo solo necesita entrenarse una vez.
		\subitem \textbf{Desventajas:} Los resultados dependen en gran medida de qué ejemplos concretos hayan caído aleatoriamente en el conjunto de prueba. Además, en conjuntos de datos pequeños, se sacrifica una cantidad valiosa de información que el modelo no podrá usar para aprender.
	\item \textbf{Validación cruzada con k pliegues (\textit{k fold cross validation}):} Consiste en subdividir el conjunto de datos original en k subconjuntos o pliegues del mismo tamaño. El proceso se repite iterativamente k veces. En cada iteración, se utiliza un pliegue distinto como conjunto de prueba y los $k - 1$ pliegues restantes se combinan para formar el conjunto de entrenamiento. Finalmente, se calcula el rendimiento global del modelo como la media aritmética de las k estimaciones obtenidas.
		\subitem \textbf{Ventajas:} Proporciona una estimación mucho más robusta, fiable y menos sesgada del rendimiento del modelo, garantizando que todos los ejemplos del \textit{dataset} original se hayan utilizado tanto para entrenar como para evaluar.
		\subitem \textbf{Desventajas:} Su coste computacional es considerablemente mayor, ya que exige entrenar y evaluar el modelo k veces.
\end{itemize}

Para el desarrollo de este proyecto, se ha diseñado una estrategia de validación híbrida y robusta que combina las virtudes de ambos enfoques, adaptándolos a la naturaleza secuencial y temporal de los datos eléctricos. Es esta validación híbrida, aplicada a la serie temporal, la que se usará en cada uno de los experimentos siguientes.

Una \textbf{serie temporal} se define como una secuencia de datos registrados en intervalos de tiempo sucesivos y generalmente regulares. A diferencia de los problemas tradicionales de modelado, donde las observaciones son independientes entre sí, en el contexto de una red de distribución eléctrica, las variables operativas presentan una fuerte dependencia cronológica. Por ello, el tratamiento de este conjunto de datos exige que su orden temporal sea preservado para evitar predecir el pasado utilizando información del futuro.

El proceso definido consta en primer lugar de una validación por retención temporal. A diferencia del método tradicional aleatorio, la partición se ha realizado respetando estrictamente el orden cronológico del \textit{dataset}. De este modo, se asigna un 80\% inicial de los datos históricos para la fase de entrenamiento de los modelos, reservando el 20\% temporalmente más reciente como un conjunto de prueba completamente independiente. Este último subconjunto se mantiene aislado hasta el final del proyecto para la evaluación final.
\begin{lstlisting}[language=Python, caption={Código de validación por retención}, label={cod:validacion_retencion}, captionpos=b]
	n_samples = X.shape[0]
	train_size = int(n_samples * 0.8)
	x_train, y_train = X[:train_size], Y[:train_size]
	x_test, y_test = X[train_size:], Y[train_size:]
\end{lstlisting}

En segundo lugar, para llevar a cabo la selección del algoritmo óptimo y el ajuste de hiperparámetros, se aplica una validación cruzada sobre el subconjunto de entrenamiento (el 80\% inicial). Dado que la aplicación de un \textit{K-Fold} tradicional es incorrecto en este escenario temporal, se ha empleado la variante de Scikit-learn \textbf{Time Series Split} \cite{scikit-learn-time-series}. 

\begin{figure}[htp]
	\centering
	\includegraphics[width=0.6\textwidth]{figures/validacion_cruzada.png}
	\caption{Validación cruzada. Fuente: \cite{scikit-learn-validacion-cruzada}}
\end{figure}
\begin{figure}[htp]
	\centering
	\includegraphics[width=0.8\textwidth]{figures/time_series_split.png}
	\caption{Time Series Split. Fuente: \cite{scikit-learn-validacion-cruzada}}
\end{figure}

Esta técnica preserva la integridad temporal mediante un enfoque de avance progresivo, donde se entrena iterativamente con una ventana de datos históricos y valida su rendimiento en el bloque temporal inmediatamente posterior.

Finalmente una vez seleccionado y entrenado el mejor modelo encontrado, se realiza una evaluación final sobre el 20\% de los datos retenidos inicialmente. Esta última prueba sobre datos jamás vistos por el algoritmo durante ninguna etapa de su desarrollo es la que garantiza que las métricas de error obtenidas sean completamente realistas y representativas de la capacidad de generalización del sistema en un entorno real de producción.

Dado que la estimación del estado de una red de distribución eléctrica es un problema de regresión múltiple, es fundamental establecer \textbf{métricas objetivas} que permitan cuantificar la diferencia entre las predicciones del modelo y los valores reales calculados. En este proyecto, se ha establecido una clara distinción entre la métrica utilizada para la optimización interna del algoritmo durante su entrenamiento y las métricas orientadas a la evaluación e interpretabilidad humana.

Para guiar el aprendizaje de los modelos durante la fase de entrenamiento, se ha seleccionado el \textbf{Error Cuadrático Medio (MSE)} como función de pérdida. Matemáticamente, se define como el promedio de los cuadrados de las diferencias entre los valores reales ($y_i$) y los valores predichos ($\hat{y}_i$). Siendo \textit{n} el número total de observaciones evaluados:
\begin{equation}
	MSE = \frac{1}{n} \sum_{i=1}^{n} (y_i - \hat{y}_i)^2
\end{equation}
La elección del MSE para el entrenamiento radica en dos motivos fundamentales. En primer lugar, al elevar el error al cuadrado, el algoritmo penaliza mucho más las grandes desviaciones. En un contexto eléctrico, predecir una tensión con un error grave es operativamente mucho más peligroso que cometer varios errores minúsculos. En segundo lugar, su superficie matemática es derivable y presenta un gradiente suave, lo que es un requisito indispensable para que algoritmos de optimización como el Descenso del Gradiente o \textit{Backpropagation} puedan actualizar los pesos de los modelos de manera eficiente y estable.

Aunque el MSE es excelente para el aprendizaje, su interpretabilidad humana es compleja. Por ello, para evaluar el rendimiento final de los modelos se han empleado tres métricas complementarias:
\begin{itemize}
	\item \textbf{Error Absoluto Medio (MAE):} Calcula el promedio de las diferencias absolutas entre las predicciones y los valores reales.
	\begin{equation}
		MAE = \frac{1}{n} \sum_{i=1}^{n} |y_i - \hat{y}_i|
	\end{equation}
	El MAE proporciona una medida del error promedio en la misma unidad física que la variable objetivo. Representa la desviación lineal esperada de cualquier predicción, ofreciendo una interpretación directa y realista del margen de error del sistema.
	\item \textbf{Error Porcentual Absoluto Medio (MAPE):} Mide el tamaño del error en términos porcentuales respecto al valor real.
	\begin{equation}
		MAPE = \frac{1}{n}\sum_{i=1}^{n}\left|\frac{y_i - \hat{y}_i}{y_i}\right|
	\end{equation}
	Dado que el \textit{dataset} incluye variables objetivo con magnitudes diferentes, el MAE no permite una comparación directa entre ellas. El MAPE soluciona este problema estandarizando el error a un porcentaje.
	\item \textbf{Coeficiente de Determinación ($R^2$):} Es una medida estadística que indica la proporción de la varianza (\textit{Var}) en la variable dependiente que es predecible a partir de las variables independientes. Es decir, un modelo con coeficiente de determinación 0 es un modelo igual de bueno que uno que solo predice la media y un modelo con coeficiente de determinación 1 indica que ha sido capaz de ajustarse a la perfección al comportamiento de estudio.
	\begin{equation}
		R^2 = 1 - \frac{MSE}{\textit{Var}(y)} = 1 - \frac{\sum_{i=1}^{n}(y_i - \hat{y}_i)^2}{\sum_{i=1}^{n}(y_i - \bar{y})^2}
	\end{equation}
	Mientras que las métricas anteriores miden la ``cantidad'' de error, $R^2$ evalúa la ``bondad de ajuste'' del modelo frente a un modelo base que solo predice la media de la población ($\bar{y}$). 
\end{itemize}

\subsection{Entrenamiento de Modelos}
En el enfoque tradicional del \textit{Machine Learning} para problemas de regresión con múltiples salidas, los algoritmos estándar tratan de predecir todas las variables objetivo de forma simultánea y paralela a partir de las variables de entrada. Sin embargo, este paradigma ignora gran parte de la naturaleza del problema eléctrico. Físicamente, la potencia reactiva inyectada por un generador ($Q_{gen7}$ en este caso) no es una variable aislada, sino que altera de forma directa el perfil de tensiones en los nudos de la red y como consecuencia también define las pérdidas del sistema.

De tal forma, para los modelos puede ser interesante aprender de esta relación entre las variables objetivo. Para resolver esta limitación, en este proyecto no solo se han evaluado modelos predictivos directos, sino que se ha diseñado e implementado una arquitectura de software propia denominada \textbf{ElectricSequentialRegressor (ESR)} basado en lo que se conoce como \textbf{Modelo Jerárquico o Cadenas de Regresores}. Esta clase está programada a medida, es completamente compatible con el ecosistema de scikit-learn y modela la red de distribución en dos fases secuenciales:
\begin{itemize}
	\item \textbf{Fase de estimación pivote (Modelo 1):} El sistema utiliza un primer estimador para predecir exclusivamente una variable clave o ``pivote'' a partir de las variables de entrada. En este caso la variable pivote es la potencia inyectada $Q_{gen7}$.
	\item \textbf{Fase de Propagación (Modelo 2):} En lugar de predecir el resto de variables partiendo de las variables de entrada, el algoritmo concatena la matriz de entrada original con la predicción obtenida en la primera fase. De este modo el segundo estimador se entrena utilizando la información previa para predecir con mayor precisión las variables más complejas (en este caso las tensiones y las pérdidas).
\end{itemize}
\begin{figure}
	\centering
	\includegraphics[width=0.9\textwidth]{figures/modelo_propio.png}
	\caption{Esquema de funcionamiento ElectricSequentialRegressor. Fuente: Elaboración propia}
\end{figure}
A nivel de software, la clase garantiza la paralelización del entrenamiento mediante la librería \textit{joblib}, reduciendo drásticamente los tiempos de cómputo en la fase de ajuste. Además, esta implementación permite utilizar modelos diferentes para cada fase, lo que puede llegar a ser muy conveniente. A nivel eléctrico, este diseño secuencial emula el proceso analítico de un flujo de carga real, obligando al modelo a asimilar que la topología de tensiones y las pérdidas son una consecuencia directa de las inyecciones de potencia previas.
\begin{lstlisting}[language=Python, caption={Código del constructor del ESR}, label={cod:esr_constructor}, captionpos=b]
	class ElectricSequentialRegressor(RegressorMixin, BaseEstimator):
		def __init__(self, estimator1=None, estimator2=None, 
			pivot_col_index=-1, n_jobs=None):
			
			self.estimator1 = estimator1
			self.estimator2 = estimator2
			self.pivot_col_index = pivot_col_index
			self.n_jobs = n_jobs
\end{lstlisting}
\begin{lstlisting}[language=Python, caption={Código del método de entrenamiento del ESR}, label={cod:esr_fit}, captionpos=b]
	def fit(self, X, y):
		assert self.estimator1 is not None 
			and self.estimator2 is not None, 
			'Must specify an estimator'
		assert is_regressor(self.estimator1) 
			and is_regressor(self.estimator2), 
			'Estimator must be a regressor'
		assert self.pivot_col_index is not None 
			and isinstance(self.pivot_col_index, int), 
			'pivot_col_index must be an integer'
		assert self.n_jobs is None 
			or isinstance(self.n_jobs, int), 
			'n_jobs must be an integer'
		
		X, y = validate_data(self, X, y, multi_output=True)
		assert y.ndim >= 2 and y.shape[1] >= 2, 
			'y must have at least 2 columns'
		self.n_outputs_ = y.shape[1]
		
		idx = self.pivot_col_index if self.pivot_col_index >= 0 
			else y.shape[1] + self.pivot_col_index
		self.pivot_real_col_index_ = idx
		y1 = y[:, idx].reshape(-1, 1)
		mask = np.ones(y.shape[1], dtype=bool)
		mask[idx] = False
		y2 = y[:, mask]
		if y2.shape[1] == 1:
			y2 = y2.ravel()
		
		models = [clone(self.estimator1), clone(self.estimator2)]
		parallel_results = Parallel(n_jobs=self.n_jobs)(
			[
				delayed(_fit_model)(models[0], X, y1.ravel()),
				delayed(_fit_model)(models[1], np.hstack([X, y1]), y2),
			]
		)
		self.model1_, self.model2_ = parallel_results
		
		return self
\end{lstlisting}
\begin{lstlisting}[language=Python, caption={Código del método de predicciones del ESR}, label={cod:esr_predictions}, captionpos=b]
	def predict(self, X):
		check_is_fitted(self, ['model1_', 'model2_'])
		
		in_data1 = validate_data(self, X, reset=False)
		predictions1 = self.model1_.predict(in_data1).reshape(-1, 1)
		in_data2 = np.hstack([in_data1, predictions1])
		predictions2 = self.model2_.predict(in_data2)
		if predictions2.ndim == 1:
			predictions2 = predictions2.reshape(-1, 1)
		
		n_samples = X.shape[0]
		predictions = np.empty((n_samples, self.n_outputs_),
			dtype=predictions1.dtype)
		predictions[:, self.pivot_real_col_index_] = predictions1.ravel()
		mask = np.ones(self.n_outputs_, dtype=bool)
		mask[self.pivot_real_col_index_] = False
		predictions[:, mask] = predictions2
		return predictions
\end{lstlisting}
\begin{lstlisting}[language=Python, caption={Ejemplo de uso del modelo ESR}, label={cod:esr_use}, captionpos=b]
	model = ElectricSequentialRegressor(
		estimator1=RandomForestRegressor(n_jobs=1, random_state=42), 
		estimator2=RandomForestRegressor(n_jobs=1, random_state=42), 
		pivot_col_index=-1, 
		n_jobs=1
	)
	model.fit(x_train, y_train)
	predictions = model.predict(x_test)
\end{lstlisting}

Para complementar y expandir el enfoque del ESR utilizando algoritmos de \textit{Deep Learning}, se ha diseñado una arquitectura de Red Neuronal Profunda basándose en la topología jerárquica de este. Utilizando las librerías \textbf{TensorFlow y Keras}, se ha construido un modelo que no se limita a procesar las entradas en una única capa de salida masiva, sino que ramifica su aprendizaje respetando la naturaleza del problema eléctrico descrito anteriormente.

Tras numerosas pruebas con esta arquitectura se llegó a la conclusión de que era necesario implementar dos variaciones que no se implementaron en el resto de modelos. Dado que el modelo estima tres salidas de naturaleza y magnitud drásticamente diferentes, el algoritmo por su propia naturaleza, ignoraba las variables más complejas y tendía a minimizar el error de las más simples. Para solucionar esto, se asignaron unos pesos específicos a la función de error de cada una de las variables (1 a las variables de las tensiones y la potencia inyectada y 2 a las pérdidas) y se normalizaron las variables objetivo entre 0 y 1 antes de realizar el entrenamiento del modelo (las predicciones por tanto estarán también normalizadas). 
\begin{lstlisting}[language=Python, caption={Código del método que crea la red neuronal}, label={cod:red_neuronal}, captionpos=b]
	def make_build_model(input_dim, num_voltages):
		def build_model(hp):
			inputs = tf.keras.Input(shape=(input_dim,), name='input_x')
			x_norm = inputs
			
			activation = hp.Choice('activation', ['relu', 'elu'])
			dropout_rate = hp.Float('dropout', 0.0, 0.3, step=0.1)
			use_batch_norm = hp.Boolean('use_batch_norm')
			learning_rate = hp.Float('lr', 1e-4, 1e-2, sampling='log')
			optimizer = hp.Choice('optimizer', values=['sgd', 'adam'])
			if optimizer == 'sgd':
				optimizer = tf.keras.optimizers.SGD(learning_rate=learning_rate)
			else:
				optimizer = tf.keras.optimizers.Adam(learning_rate=learning_rate)
			
			n_hidden_q = hp.Int('n_hidden_q', 1, 3)
			n_neurons_q = hp.Int('n_neurons_q', 32, 128, step=32)
			
			x1 = x_norm
			for _ in range(n_hidden_q):
				x1 = tf.keras.layers.Dense(n_neurons_q)(x1)
				if use_batch_norm:
					x1 = tf.keras.layers.BatchNormalization()(x1)
				x1 = tf.keras.layers.Activation(activation)(x1)
				if dropout_rate > 0:
					x1 = tf.keras.layers.Dropout(dropout_rate)(x1)
			
			out_qgen = tf.keras.layers.Dense(1, activation='linear', 
				name='out_qgen7')(x1)
			merged = tf.keras.layers.Concatenate()([x_norm, out_qgen])
			
			n_hidden_v = hp.Int('n_hidden_v', 1, 2)
			n_neurons_v = hp.Int('n_neurons_v', 64, 256, step=32)
			
			x2 = merged
			for _ in range(n_hidden_v):
				x2 = tf.keras.layers.Dense(n_neurons_v)(x2)
				if use_batch_norm:
					x2 = tf.keras.layers.BatchNormalization()(x2)
				x2 = tf.keras.layers.Activation(activation)(x2)
				if dropout_rate > 0:
					x2 = tf.keras.layers.Dropout(dropout_rate)(x2)
			
			out_v = tf.keras.layers.Dense(num_voltages, activation='linear', 
				name='out_volt')(x2)
			
			n_hidden_p = hp.Int('n_hidden_p', 2, 5)
			n_neurons_p = hp.Int('n_neurons_p', 64, 256, step=64)
			
			x3 = merged
			for _ in range(n_hidden_p):
				x3 = tf.keras.layers.Dense(n_neurons_p)(x3)
				if use_batch_norm:
					x3 = tf.keras.layers.BatchNormalization()(x3)
				x3 = tf.keras.layers.Activation(activation)(x3)
				if dropout_rate > 0:
					x3 = tf.keras.layers.Dropout(dropout_rate)(x3)
			
			out_perd = tf.keras.layers.Dense(1, activation='linear', 
				name='out_perd')(x3)
			
			model = tf.keras.Model(inputs=inputs, outputs=[out_v, 
				out_perd, out_qgen])
			
			model.compile(
				optimizer=optimizer,
				loss_weights={'out_qgen7': 1.0, 'out_volt': 1.0, 
					'out_perd': 2.0},
				loss={'out_qgen7': 'mse', 'out_volt': 'mse', 
					'out_perd': 'mse'},
				metrics={'out_qgen7': ['mae', 'mape', 
						tf.keras.metrics.R2Score(name='r2_score')],
					'out_volt': ['mae', 'mape', 
						tf.keras.metrics.R2Score(name='r2_score')],
					'out_perd': ['mae', 'mape', 
						tf.keras.metrics.R2Score(name='r2_score')]},
			)
			return model
	return build_model
\end{lstlisting}

\subsection{Optimización de Hiperparámetros}
En cuanto a la optimización de hiperparámetros, para asegurar el máximo rendimiento predictivo y la capacidad de generalización de los modelos planteados, se llevaron a cabo varios procesos de ajuste. Las estrategias seleccionadas priorizaron la eficiencia computacional (debido a la limitación de recursos en este aspecto) sin comprometer la calidad de los resultados, optando por enfoques de búsqueda aleatoria. 

Para los modelos basados en árboles de decisión, se definieron los siguientes espacios de búsqueda:
\begin{itemize}
	\item \textbf{Random Forest:} Se optimizó la complejidad estructural mediante el número de árboles \textbf{(n\_estimators)} y su profundidad máxima \textbf{(max\_depth)}. Para controlar el sobreajuste y fomentar la diversidad del bosque, se ajustó el mínimo de muestras necesarias para dividir un nodo \textbf{(min\_samples\_split)} y la proporción de variables evaluadas en cada iteración \textbf{(max\_features)}.
	\item \textbf{XGBoost:} Al tratarse de un método iterativo (boosting), la búsqueda se centró en la tasa de aprendizaje \textbf{(learning\_rate)}, que pondera la corrección que aporta cada nuevo árbol. Esto se combinó con el ajuste de la cantidad y profundidad de los árboles \textbf{(n\_estimators, max\_depth)} y el submuestreo de variables por árbol para actuar como regulador \textbf{(colsample\_bytree)}.
\end{itemize}
Dado que la arquitectura propia \textbf{ESR} se construye instanciando estos mismos algoritmos base, su proceso de optimización no requirió parámetros nuevos, sino que consistió en explorar estos mismos espacios de búsqueda por duplicado. De este modo, la búsqueda aleatoria permite optimizar el conjunto de hiperparámetros para el modelo 1 y otro conjunto independiente para el modelo 2, permitiendo que cada fase adaptara su complejidad a la dificultad de su tarea.

Para la exploración de estos conjuntos, se utilizó la implementación \textbf{RandomizedSearchCV} de la biblioteca \textit{scikit-learn}. Esto permitió explorar distribuciones de valores mediante un muestreo aleatorio dentro de rangos predefinidos, evaluando el rendimiento a través de validación cruzada. 
\begin{lstlisting}[language=Python, caption={Código de exploración de hiperparámetros con ESR\_RandomForest}, label={cod:hiperparam_random_forest}, captionpos=b]
	random_model = ElectricSequentialRegressor(
		RandomForestRegressor(n_jobs=1, random_state=42),
		RandomForestRegressor(n_jobs=1, random_state=42))
	random_grid = {
		'estimator1__n_estimators': randint(30, 101),
		'estimator1__max_depth': randint(10, 51),
		'estimator1__min_samples_split': randint(2, 21),
		'estimator1__max_features': uniform(0.1, 0.9),
		'estimator2__n_estimators': randint(80, 251),
		'estimator2__max_depth': randint(10, 51),
		'estimator2__min_samples_split': randint(2, 21),
		'estimator2__max_features': uniform(0.1, 0.9)
	}
	random_scoring = {
		'mse_global': 'neg_mean_squared_error',
		'mse_v_perd': make_scorer(mse_column_score, 
			col_idx=[c for c in range(0, Y.shape[1] - 1)], 
			greater_is_better=False),
		'mse_qgen7': make_scorer(mse_column_score, 
			col_idx=-1, greater_is_better=False),
	}
	random_search = RandomizedSearchCV(random_model, 
		random_grid, n_iter=30,
		cv=TimeSeriesSplit(n_splits=3), 
		scoring=random_scoring, refit='mse_global',
		n_jobs=-1, verbose=2, random_state=42, 
		return_train_score=True
	)
	
	random_search.fit(x_train, y_train)
\end{lstlisting}
\begin{lstlisting}[language=Python, caption={Código de exploración de hiperparámetros con ESR\_XGBoost}, label={cod:hiperparam_xgboost}, captionpos=b]
	random_model = ElectricSequentialRegressor(
		XGBRegressor(objective='reg:squarederror', n_jobs=1, random_state=42), 
		XGBRegressor(objective='reg:squarederror', n_jobs=1, random_state=42), 
		n_jobs=1
	)
	random_grid = {
		'estimator1__n_estimators': randint(30, 101),
		'estimator1__max_depth': randint(3, 9),
		'estimator1__learning_rate': uniform(0.01, 0.2),
		'estimator1__colsample_bytree': uniform(0.4, 0.5),
		'estimator2__n_estimators': randint(80, 251),
		'estimator2__max_depth': randint(4, 11),
		'estimator2__learning_rate': uniform(0.01, 0.2),
		'estimator2__colsample_bytree': uniform(0.4, 0.5)
	}
	random_scoring = {
		'mse_global': 'neg_mean_squared_error',
		'mse_v_perd': make_scorer(mse_column_score, 
			col_idx=[c for c in range(0, Y.shape[1] - 1)], 
			greater_is_better=False),
		'mse_qgen7': make_scorer(mse_column_score, 
			col_idx=-1, greater_is_better=False),
	}
	random_search = RandomizedSearchCV(random_model, random_grid, 
		n_iter=30, cv=TimeSeriesSplit(n_splits=3), 
		scoring=random_scoring, refit='mse_global',
		n_jobs=-1, verbose=2, random_state=42, 
		return_train_score=True
	)
	
	random_search.fit(x_train, y_train)
\end{lstlisting}

Por último, en la Red Neuronal Profunda, para la topología estructural, se permitió buscar de forma independiente el número de capas ocultas y la cantidad de neuronas para cada una de las tres ramas predictivas (potencia reactiva, tensiones y pérdidas). Este enfoque permite que la red asigne dinámicamente mayor capacidad matemática a las variables de mayor complejidad física. Asimismo, se exploraron técnicas de regularización como la tasa de \textit{Dropout} y la inclusión de \textit{Batch Normalization}, junto con la selección de la función de activación (ReLU o ELU). Para el algoritmo de entrenamiento, se evaluaron los optimizadores Adam y SGD \cite{keras-layers}. 

Para evaluar estas combinaciones de forma rigurosa, se programó una clase a medida denominada \textbf{TimeSeriesRandomSearch}, basada en la librería \textit{Keras Tuner}. Esta implementación realiza una búsqueda aleatoria de hiperparámetros utilizando validación cruzada temporal (\textit{TimeSeriesSplit}), asegurando que siempre se respete el orden cronológico de los datos. Además, previene la fuga de información al escalar las variables (\textit{MinMaxScaler}) de forma aislada dentro de cada iteración del pliegue, evitando que el modelo ``vea'' la estadística de datos futuros durante su entrenamiento.
\begin{lstlisting}[language=Python, caption={Código de exploración de hiperparámetros con Redes Neuronales}, label={cod:red_neuronal_random}, captionpos=b]
	class TimeSeriesRandomSearch(kt.RandomSearch):
		def run_trial(self, trial, x, y, n_splits=5, 
			batch_size=32, epochs=10, callbacks=None, **kwargs):
			
			time_series = TimeSeriesSplit(n_splits=n_splits)
			
			metrics_fold = []
			for fold, (train_index, test_index) 
				in enumerate(time_series.split(x)):
				
				model = self.hypermodel.build(trial.hyperparameters)
				if hasattr(x, 'iloc'):
					x_train_fold, x_test_fold = x.iloc[train_index], 
						x.iloc[test_index]
				else:
					x_train_fold, x_test_fold = x[train_index],
						x[test_index]
						
				scaler_x = MinMaxScaler()
				x_train_fold = scaler_x.fit_transform(x_train_fold)
				x_test_fold = scaler_x.transform(x_test_fold)
				
				y_train_fold = {}
				y_test_fold = {}
				for key, data in y.items():
					if hasattr(data, 'iloc'):
						y_train_data = data.iloc[train_index]
						y_test_data = data.iloc[test_index]
					else:
						y_train_data = data[train_index]
						y_test_data = data[test_index]
					
					normalizer_y = MinMaxScaler()
					y_train_values = y_train_data.values 
						if hasattr(y_train_data, 'values') else y_train_data
					y_test_values = y_test_data.values 
						if hasattr(y_test_data, 'values') else y_test_data
					
					y_train_fold[key] = normalizer_y.
						fit_transform(y_train_values)
					y_test_fold[key] = normalizer_y.
						transform(y_test_values)
				
				model.fit(x_train_fold, y_train_fold, 
					validation_data=(x_test_fold, y_test_fold),
				batch_size=batch_size, epochs=epochs, 
					verbose=0, callbacks=callbacks)
				metrics = model.evaluate(x_test_fold, y_test_fold,
					verbose=0, return_dict=True)
				metrics_fold.append(metrics)
				
				print(f'Trial {trial.trial_id} - Fold {fold + 1}:')
				for metric, value in metrics.items():
					print(f'\t {metric}: {value:.5f}')
			
			avg_metrics = {}
			for metric_name in metrics_fold[0].keys():
				values = [metrics[metric_name] for metrics in metrics_fold]
				key_name = f'val_{metric_name}'
				avg_metrics[key_name] = np.mean(values)
			print(f"--- Promedio Trial {trial.trial_id}: 
				val_loss = {avg_metrics['val_loss']:.5f} ---\n")
			self.oracle.update_trial(trial.trial_id, avg_metrics)
\end{lstlisting}

Durante la fase de experimentación, se evaluó el uso de \textit{GridSearchCV} (búsqueda en rejilla y exhaustiva). Sin embargo, tras ejecuciones de hasta 5 horas, la mejora en el rendimiento de los modelos fue muy escasa, limitándose a variaciones de apenas unos decimales. Este comportamiento obligó a utilizar búsquedas menos costosas computacionalmente, en este caso la búsqueda aleatoria que permite una mayor cobertura de combinación de parámetros en menos tiempo.

Adicionalmente a las estrategias de búsqueda aleatorias descritas, se han explorado métodos alternativos más avanzados para la optimización de hiperparámetros, destacando el uso de la librería \textbf{MetaGen}. Esta herramienta es un \textit{framework} de Python diseñado específicamente para aplicar \textbf{algoritmos metaheurísticos} en la optimización de modelos. Su uso se exploró por su gran capacidad computacional para converger hacia configuraciones óptimas de manera mucho más eficiente al enfrentarse a arquitecturas complejas.